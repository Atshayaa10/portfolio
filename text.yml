name: Test CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Failing test
        run: |
          echo "This will fail"
          exit 1
